---
// The MDX lives in _load-testing.mdx (underscore = not routed) and is wrapped
// here, so the layout receives frontmatter explicitly — same pattern as
// testing.astro wrapping _testing.mdx.
import type { ComponentProps } from "astro/types";
import Doc from "../layouts/Doc.astro";
import LoadTesting, { frontmatter } from "./_load-testing.mdx";

const fm = frontmatter as ComponentProps<typeof Doc>["frontmatter"];
---
<Doc frontmatter={fm}>
  <LoadTesting />
</Doc>
