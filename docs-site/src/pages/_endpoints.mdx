---
title: "Every endpoint, <em>down to the Redis command</em>"
summary: "Each Durable Streams endpoint Chronicle serves: PUT, POST, GET, HEAD, DELETE. For each one, this page shows a request, the handler and store flow, and the Redis commands that run."
eyebrow: "Reference"
meta: "Endpoints & Redis"
deck: "Chronicle maps the <strong>Durable Streams</strong> protocol onto five HTTP verbs. Mutations run in atomic Lua scripts. Reads use a typed, bounded page that carries both frames and the response snapshot. Each section follows one endpoint through the handler, store, and Redis commands in execution order."
byline:
  - label: "Verbs"
    value: "PUT · POST · GET · HEAD · DELETE"
  - label: "Scripts"
    value: "7 operations + shared prelude"
  - label: "Source"
    value: "store/redis"
    href: "https://github.com/adityavkk/chronicle/tree/main/store/redis"
---
import Tldr from "../components/Tldr.astro";
import SectionHead from "../components/SectionHead.astro";
import Callout from "../components/Callout.astro";
import Table from "../components/Table.astro";
import Diagram from "../components/Diagram.astro";
import RedisTrace from "../components/RedisTrace.astro";
import Complexity from "../components/Complexity.astro";
import Aside from "../components/Aside.astro";

<Tldr>
  <ul>
    <li><strong>Five verbs, with atomic mutations and bounded reads.</strong> <code>PUT</code> creates, <code>POST</code> appends or closes, <code>GET</code> reads, <code>HEAD</code> probes, and <code>DELETE</code> removes. Each mutation script validates and commits as one Redis operation. <code>GET</code> and <code>HEAD</code> use <code>read.lua</code> to return a typed snapshot without loading producer state.</li>
    <li><strong>The same four parts every time.</strong> The HTTP handler parses the request. The Go store frames bytes and owns typed results. Redis runs the required script. Pure Go functions define semantic decisions, and Lua mirrors the decisions that must run atomically.</li>
    <li><strong>Reads use bounded <code>ZRANGEBYLEX</code> pages.</strong> Frames are stored as <code>"&lt;offset&gt;|&lt;bytes&gt;"</code> members in a sorted set, zero-padded so byte order equals stream order. The first page captures the response tail. Redis fetches one candidate, then optionally bulk-fetches only the offset range that fits the remaining 1 MiB target. A separate 1,024-frame cap bounds the page.</li>
    <li><strong>Live tailing is pub/sub plus a typed re-read.</strong> A long-poll performs one logical read, subscribes, then makes no-touch rechecks before blocking and after each wake. The final page supplies the timeout or wake response. The handler does not fetch metadata again.</li>
    <li><strong>An append response does not run subscription fanout.</strong> After a new append commits, the handler performs one bounded local dirty-queue transition. Subscriber lookup, cursor hydration, linked-tail reads, and wake arming run in a Manager-owned worker. Failed appends and producer duplicates create no dirty hint.</li>
    <li><strong>Ordinary persistent reads do not write.</strong> For a valid non-expiring stream, <code>GET</code>, <code>HEAD</code>, long-poll rechecks, and SSE refreshes issue no <code>HSET</code>, <code>HSETNX</code>, <code>PEXPIRE</code>, or <code>PERSIST</code>. Sliding TTL reads renew once at the first root snapshot. Lazy expiry cleanup and one-time legacy incarnation migration remain explicit write cases.</li>
  </ul>
</Tldr>

<section class="block" id="how-to-read">
  <SectionHead num="00" label="Orientation" title="How to read <em>this page</em>" />

  Every section below has the same four parts. A request you can `curl`, a
  sequence diagram, the Redis command sequence the script runs, and a sentence
  on each command.

  ### Four <em>parts</em>

  A request crosses the same four boundaries on every endpoint.

  <Table>
    | Part | Is | Does |
    | --- | --- | --- |
    | **Client** | `curl`, a browser, an official client library | Sends an HTTP request, stores one offset string to resume |
    | **Handler** | `handler.go` (Go) | Parses headers and query, calls the store, maps store errors to HTTP status codes |
    | **Store** | `store/redis/store.go` (Go) | Frames bytes, captures typed read snapshots, runs Lua scripts, and decodes raw Redis replies at one boundary |
    | **Redis** | Redis 8 | Runs one Lua script per mutation, atomically and serialized per stream |
  </Table>

  ### Keys and the <em>notify channel</em>

  Each stream at path `p` is four Redis keys that share a `{p}` hash tag, so
  they occupy one cluster slot, plus one pub/sub channel.

  <Table>
    | Key | Type | Holds |
    | --- | --- | --- |
    | `ds:{p}:meta` | hash | content type, tail offset, closed flag, TTL/expiry, fork lineage, refcount |
    | `ds:{p}:msg` | sorted set | message frames `"<offset>\|<bytes>"`, score `0`, ordered lexicographically |
    | `ds:{p}:prod` | hash | idempotent-producer state: `producerId → "epoch:lastSeq:lastUpdated"` |
    | `ds:{p}:forks` | set | the paths of forks that reference this stream as their source |
    | `ds:notify:{p}` | pub/sub | wakes long-poll and SSE waiters: `a` append, `c` close, `d` delete |
  </Table>

  <Aside label="Encoding" summary="How offsets and frames are laid out for lexicographic reads">
    An offset is two unsigned 64-bit numbers, `ReadSeq_ByteOffset`, each
    zero-padded to 16 digits. A fresh tail is
    `0000000000000000_0000000000000000`.[^offset] `ReadSeq` is reserved for
    future log rotation and stays `0`. `ByteOffset` counts payload bytes, not
    framing. A frame is one sorted-set member holding the message end offset,
    a `|`, then the raw payload, e.g. `0_13|{"tok":"The"}`.[^frame] Because
    the prefix is fixed-width and zero-padded, bytewise order equals stream
    order. Each storage page uses `ZRANGEBYLEX` from an exclusive lower bound
    to the captured snapshot tail. This page abbreviates offsets to their
    significant digits (`0_0`, `0_13`, `0_29`). On the wire both halves are
    full width.
  </Aside>

  <Aside label="Shared machinery" summary="Three things that recur inside the mutation scripts">
    <ul>
      <li><strong>Lazy expiry.</strong> The first script to touch an expired stream runs <code>expire_cleanup</code> (a <code>DEL</code>, or a soft-delete if forks still reference it) and reports not found. Expiry is checked on access. There is no sweeper.</li>
      <li><strong>The sliding-TTL touch.</strong> An append updates <code>accessedAtNs</code>. A logical client read does so once, at its first root snapshot, and only when the stream has a sliding TTL. A non-expiring read, an absolute-expiry-only read, <code>HEAD</code>, a continuation page, a long-poll recheck, an inherited source range, and an internal sealing or repair read do not renew access.</li>
      <li><strong>The TTL backstop.</strong> <code>refresh_backstop</code> arms a key TTL of <code>ttl × 1000 + 60000</code> ms via <code>PEXPIRE</code>, a 60-second grace over the logical TTL. A fork-referenced or soft-deleted stream gets <code>PERSIST</code> instead.</li>
    </ul>
    Mutation replies use a fixed nine-field string array so int64 values
    survive Lua's double round-trip. Read replies instead carry one metadata
    field array, one bounded member array, and byte counts.[^reply]
  </Aside>

  <Aside label="Cost model" summary="Why each endpoint costs what it costs. Three Redis structures carry the work">
    The `:msg` sorted set is the only structure that grows without bound. Past
    Redis's listpack threshold it is a skiplist, so an insert is `O(log N)`
    and a lexicographic range is `O(log N + M)`.[^zset] The `:meta` and
    `:prod` hashes are `O(1)` per field. The `:forks` set is `O(1)` to add or
    remove, and a notify publish is `O(S)` in subscribers. Throughout:
    **N** frames in the stream, **M** frames the call touches, **k** messages
    per batch, **b** bytes per batch, **S** subscribers, **P** producers,
    **d** fork-chain depth.
  </Aside>

  <Callout icon="arrow" label="Subscriptions">
    This page covers the stream operations: five verbs, one Lua script each.
    The protocol's reserved subscription API (`__ds` webhook and pull-wake
    delivery) is also implemented, on a separate plane with its own
    durability story, on [its own page](../subscriptions).
  </Callout>
</section>

<section class="block" id="put-create">
  <SectionHead num="01" label="PUT" title="Create a stream" />

  `PUT {stream-url}` creates a stream, and it is idempotent. A repeated `PUT`
  with matching configuration returns `200` instead of `201`. A `PUT` whose
  content type, TTL, or closed status differs returns `409`.[^create] The body,
  if any, becomes the stream's initial content.

  ```http title="Create a JSON stream"
  PUT /v1/stream/chat HTTP/1.1
  Content-Type: application/json
  Stream-TTL: 3600

  HTTP/1.1 201 Created
  Location: /v1/stream/chat
  Content-Type: application/json
  Stream-Next-Offset: 0000000000000000_0000000000000000
  ```

  <Diagram
    name="seq-create"
    label="PUT, create"
    caption="The pre-check (step 3) and the script's own existence check (inside step 4) run the same logic twice. Once to honor upstream's error precedence, once atomically."
  />

  <RedisTrace
    script="create.lua"
    source="store/redis/scripts/create.lua"
    steps={[
      { cmd: "HGETALL", key: "ds:{p}:meta", does: "Load the meta hash. An empty reply means the path is free, so creation continues. A present hash short-circuits. Matching config returns <code>MATCHED</code> (the handler answers <strong>200</strong>), a mismatch returns <code>MISMATCH</code> (<strong>409</strong>), and a soft-deleted or still-referenced expired stream returns <code>EXISTS</code> (<strong>409</strong>).", loc: "create.lua:40" },
      { cmd: "DEL", key: "ds:{p}:meta · :msg · :prod · :forks", does: "Defensive. If this same script reaped an expired stream a few lines up, this clears the residue before the fresh write. On a truly new path it is a no-op.", loc: "create.lua:57" },
      { cmd: "HSET", key: "ds:{p}:meta", does: "Write all metadata in one call: <code>ct</code>, <code>tail = 0_0</code>, <code>createdAtNs</code>, <code>accessedAtNs</code>, and <code>ttl</code> or <code>expAtNs</code> when supplied.", loc: "create.lua:62" },
      { cmd: "ZADD", key: "ds:{p}:msg", when: "only with a body", does: "Add each initial frame at score <code>0</code>, chunked 1000 members at a time. Members sort by their zero-padded offset prefix, so insertion order is stream order.", loc: "create.lua:69" },
      { cmd: "PUBLISH", key: "ds:notify:{p}", when: "only with a body", does: "Send <code>a</code> to wake any reader already long-polling the new path.", loc: "create.lua:77" },
      { cmd: "PEXPIRE", key: "ds:{p}:*", does: "<code>refresh_backstop</code> arms the GC key TTL of <code>ttl × 1000 + 60000</code> ms on all four keys, or runs <code>PERSIST</code> on them when the stream has no TTL.", loc: "create.lua:81" },
    ]}
    caption="Every command runs inside one EVALSHA, so a concurrent reader can never observe a half-built stream: meta without frames, or frames without a tail."
  />

  <Callout icon="info" label="Subscription hooks after create">
    A genuinely new stream runs the pattern-linking hook after `create.lua`
    commits. If the create has initial data, Chronicle runs that hook first, then
    records exactly one dirty append hint. The background fanout
    worker can therefore see subscriptions that match the new path. A repeated
    idempotent `PUT` runs neither hook. Pattern linking remains part of the create
    response path; the async queue changes append fanout only.
  </Callout>

  <Complexity
    time="O(1) empty · O(k log k) with a k-message body"
    space="O(1) · O(b) for a b-byte body"
    why="An empty create is a few O(1) hash writes plus a constant set of TTL commands. A create with a body inserts each of the k initial messages into a <em>fresh</em> <code>:msg</code> skiplist, which is k × O(log k), and stores the b payload bytes. The existence pre-check reads only the bounded meta hash, so it stays O(1)."
  />

  ### Forking: <em>two streams, two slots</em>

  A `PUT` carrying `Stream-Forked-From` creates a fork. A fork is a new
  stream that inherits the source's data up to a fork offset without copying
  it.[^fork] This is the one operation that is not a single atomic step,
  because the fork's keys and the source's keys live in different cluster
  slots. The store runs two scripts: `incr_ref.lua` takes a reference on the
  source, then `create.lua` builds the child.

  ```http title="Fork an existing stream at offset 0_13"
  PUT /v1/stream/branch HTTP/1.1
  Stream-Forked-From: /v1/stream/chat
  Stream-Fork-Offset: 0000000000000000_0000000000000013

  HTTP/1.1 201 Created
  Stream-Next-Offset: 0000000000000000_0000000000000013
  ```

  <Diagram
    name="seq-fork"
    label="PUT, fork"
    caption="For reads below forkOff, Go plans a finite source segment and runs the bounded read script against the source's :msg in its own slot, capped at the fork point. The fork's own appends extend it past forkOff."
  />

  <RedisTrace
    script="incr_ref.lua"
    source="store/redis/scripts/incr_ref.lua"
    steps={[
      { cmd: "HGETALL", key: "source:meta", does: "Re-validate the source atomically. Go checked a snapshot first. Absent → <code>NOTFOUND</code> (<strong>404</strong>), soft-deleted → <code>SOFTDEL</code> (<strong>409</strong>), expired → cleanup then <code>NOTFOUND</code>.", loc: "incr_ref.lua:13" },
      { cmd: "HINCRBY", key: "source:meta refCount 1", does: "Record that one more fork depends on this source's inherited prefix. The refcount is what blocks a plain delete and forces a soft-delete instead.", loc: "incr_ref.lua:23" },
      { cmd: "SADD", key: "source:forks /v1/stream/branch", does: "Register the child path in the source's fork set, so the lineage can be walked for garbage collection.", loc: "incr_ref.lua:24" },
      { cmd: "PERSIST", key: "source:*", does: "Strip the TTL backstop from all four source keys. A stream a fork reads through must not be reaped while the fork lives.", loc: "incr_ref.lua:25" },
    ]}
    caption="Then create.lua runs as in §01, except the child's meta records forkedFrom and the resolved forkOff."
  />

  <Complexity
    label="Complexity, what forking saves"
    time="O(1)"
    space="O(1), no data copied"
    why="Forking copies nothing, however long the source. <code>incr_ref</code> bumps a refcount, registers the child, and persists the source keys, and <code>create</code> writes the fork's own meta, all O(1). The inherited prefix stays in the source's skiplist and is range-scanned in place on a read. A <code>Stream-Fork-Sub-Offset</code> adds one O(log N + M) read of the source to resolve the boundary byte."
  />

  <Callout icon="alert" label="Non-atomic across the pair">
    Because the two scripts span two slots, a crash between `incr_ref.lua`
    and `create.lua` leaks one reference on the source. That reference is
    reaped later by delete-cascade arithmetic, never by a dangling read. A
    concurrent identical fork is resolved by rolling our extra reference
    back. This window is documented. Everything within a single stream
    stays atomic.
  </Callout>
</section>

<section class="block" id="post-append">
  <SectionHead num="02" label="POST" title="Append to a stream" />

  `POST {stream-url}` with a body appends one batch. In JSON mode the body
  is a JSON array, flattened one level into individual messages. Otherwise
  the whole body is one message.[^append] The store frames the bytes against a
  tail snapshot, then runs `append.lua`, which re-validates and writes in one
  atomic step. A concurrent append that moved the tail returns `RETRY`, and the
  store re-frames up to 64 times before giving up.

  ```http title="Append two JSON messages"
  POST /v1/stream/chat HTTP/1.1
  Content-Type: application/json

  [{"tok":"The"},{"tok":"answer"}]

  HTTP/1.1 204 No Content
  Stream-Next-Offset: 0000000000000000_0000000000000029
  ```

  The batch flattens to `{"tok":"The"}` (13 bytes) and `{"tok":"answer"}`
  (16 bytes), with end offsets `0_13` and `0_29`, so the tail advances to
  `0_29`.

  <Diagram
    name="seq-append"
    label="POST, append"
    caption="On RETRY the store loops back to the HMGET snapshot and re-frames against the new tail. Once a new append commits, the handler records one bounded local dirty hint and returns. Subscription fanout runs in the background."
  />

  <Callout icon="arrow" label="The subscription boundary">
    `Store.Append` is the durable commit point. A genuinely new append then calls
    `OnStreamAppend`, which performs one fixed-capacity local queue transition
    and one nonblocking worker signal. It does not call Redis, enumerate
    subscribers, load subscription state, read linked tails, arm wakes, or
    deliver a webhook. The HTTP response keeps its existing status, headers, and
    body even if later fanout fails. The recovery sweep derives any missed wake
    from the durable stream tail and subscription cursor. See [the complete
    queue and failure policy](../performance-improvements/async-subscription-fanout).
  </Callout>

  <RedisTrace
    script="append.lua"
    source="store/redis/scripts/append.lua"
    steps={[
      { cmd: "HGETALL", key: "ds:{p}:meta", does: "Load meta. Absent → <code>NOTFOUND</code> (<strong>404</strong>). This is the existence gate before any write.", loc: "append.lua:35" },
      { cmd: "is_expired?", does: "Soft-deleted → <code>SOFTDEL</code> (<strong>410</strong>), checked before expiry to match the memory store's order. Then lazy expiry: past <code>expAtNs</code> or <code>accessedAtNs + ttl</code> → cleanup and <code>NOTFOUND</code>.", loc: "append.lua:39", kind: "flow" },
      { cmd: "HSET", key: "ds:{p}:meta accessedAtNs", does: "The sliding-TTL touch. Upstream touches before the closed check, so even an append about to be rejected slides the idle window forward.", loc: "append.lua:50" },
      { cmd: "PEXPIRE", key: "ds:{p}:*", does: "Re-arm the backstop against the new <code>accessedAtNs</code>.", loc: "append.lua:51" },
      { cmd: "validate", does: "In spec precedence: closed stream → <code>CLOSED</code> (<strong>409</strong> + <code>Stream-Closed</code>) unless the body duplicates the closing producer tuple, then content-type mismatch → <code>CTMISMATCH</code> (<strong>409</strong>), then <code>Stream-Seq ≤ lastSeq</code> → <code>SEQCONFLICT</code> (<strong>409</strong>).", loc: "append.lua:55", kind: "flow" },
      { cmd: "tail == expected?", does: "The optimistic check. Go framed the bytes against a tail snapshot; if a concurrent append moved the tail, return <code>RETRY</code> and let Go re-frame.", loc: "append.lua:93", kind: "flow" },
      { cmd: "ZADD", key: "ds:{p}:msg", does: "Write each frame <code>\"&lt;endOffset&gt;|&lt;bytes&gt;\"</code> at score <code>0</code>, chunked 1000 at a time (Lua's <code>unpack</code> is C-stack bounded).", loc: "append.lua:104" },
      { cmd: "HSET", key: "ds:{p}:meta tail", does: "Advance the tail to the new end offset, in the same script as the frame writes.", loc: "append.lua:108" },
      { cmd: "PUBLISH", key: "ds:notify:{p}", does: "Send <code>a</code> (or <code>c</code> when the append also closes), waking every long-poll and SSE waiter on the stream.", loc: "append.lua:139" },
    ]}
  />

  <Complexity
    time="O(k log N) commit · O(1) subscription handoff"
    space="O(b)"
    why="Each of the k messages in the batch is one skiplist insert into a <code>:msg</code> of current size N, which is O(log N) apiece. The tail update and producer fence are O(1), and the Redis publish is O(S) in live stream waiters. After commit, the subscription hook performs one bounded map and ring transition whose cost does not grow with subscription fanout. Space grows by the b appended bytes. The optimistic retry loop multiplies the <em>number</em> of attempts under contention, not the cost of one."
  />

  ### With an <em>idempotent producer</em>

  Three optional headers, `Producer-Id`, `Producer-Epoch`, and
  `Producer-Seq`, turn an append into a write that the server deduplicates on
  retry.[^producer] They must all be present or all absent. Producer
  validation runs **before** the `Stream-Seq` check, so a retry deduplicates
  even when its sequence would otherwise conflict, and a duplicate returns
  with **no write**.

  ```http title="Append with producer headers"
  POST /v1/stream/chat HTTP/1.1
  Content-Type: application/json
  Producer-Id: writer-1
  Producer-Epoch: 0
  Producer-Seq: 7

  [{"tok":"now"}]

  HTTP/1.1 200 OK
  Producer-Epoch: 0
  Producer-Seq: 7
  Stream-Next-Offset: 0000000000000000_0000000000000033
  ```

  <RedisTrace
    label="The producer branch, step by step"
    script="append.lua, producer branch"
    source="store/redis/scripts/append.lua"
    steps={[
      { cmd: "HGET", key: "ds:{p}:prod writer-1", does: "Read this producer's stored <code>\"epoch:lastSeq:lastUpdated\"</code>, or nil on first contact.", loc: "append.lua:71" },
      { cmd: "validate_producer", does: "The spec's §5.2.1 state machine, transcribed. <code>seq ≤ lastSeq</code> → <em>Duplicate</em> (no write), then <code>epoch &lt; stored</code> → <em>StaleEpoch</em>, then <code>epoch &gt; stored</code> with <code>seq ≠ 0</code> → <em>EpochSeq</em>, then <code>seq &gt; lastSeq + 1</code> → <em>SeqGap</em>, otherwise <em>Accept</em>.", loc: "common.lua:89", kind: "flow" },
      { cmd: "ZADD · HSET", key: "ds:{p}:msg · :meta", does: "On <em>Accept</em> only, the frame and tail writes happen exactly as in the plain path. A <em>Duplicate</em> skips straight to the reply.", loc: "append.lua:96", kind: "write" },
      { cmd: "HSET", key: "ds:{p}:prod writer-1", does: "Advance the fence to <code>\"0:7:&lt;unixSec&gt;\"</code>, the tuple the next sequence is validated against.", loc: "append.lua:132" },
      { cmd: "PUBLISH", key: "ds:notify:{p}", does: "Send <code>a</code> on an accepted write.", loc: "append.lua:139" },
    ]}
  />

  <Table>
    | Verdict | Condition | HTTP | Extra headers |
    | --- | --- | --- | --- |
    | **Accept** | new `(epoch, seq)` in order | `200 OK` | `Producer-Epoch`, `Producer-Seq` |
    | **Duplicate** | `seq ≤ lastSeq` | `204 No Content` | echoes the stored `Producer-Seq` |
    | **StaleEpoch** | `epoch < stored` (a producer fenced by a newer epoch) | `403 Forbidden` | `Producer-Epoch: <current>` |
    | **SeqGap** | `seq > lastSeq + 1` | `409 Conflict` | `Producer-Expected-Seq`, `Producer-Received-Seq` |
    | **EpochSeq** | new epoch not starting at `seq 0` | `400 Bad Request` | none |
  </Table>

  A producer **Accept** result records one dirty hint after commit. A
  **Duplicate** returns before the hook because it wrote no new stream data. All
  error verdicts also skip the hook, so subscription work cannot change append
  error precedence.

  ### Append <em>and close</em>

  Add `Stream-Closed: true` to an append with a body and the same
  `append.lua` writes the final frame, flips the closed flag, and publishes
  `c` instead of `a` in one step. Readers see the last bytes and the EOF
  together. Because the tail moved, Chronicle records one dirty subscription
  hint after the commit. The close-only endpoint below does not record an append
  hint because it writes no data.
</section>

<section class="block" id="post-close">
  <SectionHead num="03" label="POST" title="Close a stream" />

  `POST {stream-url}` with `Stream-Closed: true` and an empty body closes a
  stream without appending. Closure is durable and monotonic. Once closed, a
  stream stays closed, and every later append returns `409`.[^close] The
  close-only path is its own script, `close.lua`.

  ```http title="Close-only"
  POST /v1/stream/chat HTTP/1.1
  Stream-Closed: true

  HTTP/1.1 204 No Content
  Stream-Closed: true
  Stream-Next-Offset: 0000000000000000_0000000000000029
  ```

  <Diagram
    name="seq-close"
    label="POST, close"
    caption="The tail does not move, because close appends nothing. A close with producer headers takes the CloseStreamWithProducer path, which records the closing tuple for dedup."
  />

  <RedisTrace
    script="close.lua"
    source="store/redis/scripts/close.lua"
    steps={[
      { cmd: "HGETALL", key: "ds:{p}:meta", does: "Load meta. Absent → <code>NOTFOUND</code> (<strong>404</strong>).", loc: "close.lua:19" },
      { cmd: "is_expired?", does: "Expired → cleanup and <code>NOTFOUND</code>. Note what is <em>missing</em>. There is no sliding-TTL touch and no soft-delete check, because close is deliberately not access.", loc: "close.lua:22", kind: "flow" },
      { cmd: "already closed?", does: "Idempotent. A plain re-close still publishes <code>c</code> and returns <code>OK</code> with <code>alreadyClosed = 1</code>. A producer re-close matching the recorded closing tuple is a dedup success. A different tuple → <code>CLOSED</code> (<strong>409</strong>).", loc: "close.lua:27", kind: "flow" },
      { cmd: "HSET", key: "ds:{p}:meta closed 1", does: "Flip the terminal, monotonic closed flag. Every subsequent append now returns <strong>409</strong> with <code>Stream-Closed: true</code>.", loc: "close.lua:64" },
      { cmd: "PUBLISH", key: "ds:notify:{p}", does: "Send <code>c</code> so every parked reader observes EOF immediately instead of hanging to the long-poll timeout.", loc: "close.lua:65" },
    ]}
    caption="A producer close adds two writes before the publish. HSET :prod advances the producer fence, and the cbId/cbEpoch/cbSeq fields on :meta let a duplicate close request be recognized."
  />

  <Complexity
    time="O(1)"
    space="O(1)"
    why="Close flips one hash field (<code>HSET closed 1</code>) and publishes a wakeup, which is O(1), plus O(S) to notify the S parked readers. No frames are read or written, and the tail does not move."
  />
</section>

<section class="block" id="get-catchup">
  <SectionHead num="04" label="GET" title="Read, catch-up" />

  `GET {stream-url}?offset=<offset>` returns messages past the offset through
  the tail captured by the first storage page. `offset=-1` (or an absent
  offset) starts from the beginning, and `offset=now` resolves to the current
  tail.[^read] Chronicle reads frame-aligned pages and writes each page directly
  to the response before fetching the next. Concurrent appends are left for the
  next response.

  ```http title="Read from the beginning"
  GET /v1/stream/chat?offset=-1 HTTP/1.1

  HTTP/1.1 200 OK
  Content-Type: application/json
  Stream-Next-Offset: 0000000000000000_0000000000000029
  ETag: "0000000000000000_0000000000000029"
  Cache-Control: public, max-age=60, stale-while-revalidate=300

  [{"tok":"The"},{"tok":"answer"}]
  ```

  <Diagram
    name="seq-read"
    label="GET, catch-up read"
    caption="ETag is the next offset, so a conditional re-read at the same position answers 304. A read behind the tail is cacheable for 60 s. A read at the tail is not."
  />

  <RedisTrace
    script="read.lua"
    source="store/redis/scripts/read.lua"
    steps={[
      { cmd: "EVALSHA + HGETALL", key: "ds:{p}:meta", does: "The root validation invocation loads metadata once. Absent → <code>NOTFOUND</code> (<strong>404</strong>). The first page captures tail, content type, closed state, expiry configuration, fork boundary, and incarnation. It never reads <code>:prod</code>.", loc: "read.lua:32", kind: "read" },
      { cmd: "expiry · softDel", does: "An expired root runs lazy cleanup and reports <code>NOTFOUND</code>. A soft-deleted root reports <code>SOFTDEL</code>, which the GET path hides as <strong>404</strong>.", loc: "read.lua:35", kind: "flow" },
      { cmd: "HSETNX", key: "ds:{p}:meta incarnation", does: "A legacy stream without an incarnation stores one random identity once. New streams skip this branch.", when: "legacy metadata only", loc: "read.lua:43", kind: "write" },
      { cmd: "incarnation matches?", does: "A continuation page whose path now names a recreated stream returns <code>SNAPSHOT</code>. One HTTP response cannot combine two stream lifetimes.", loc: "read.lua:56", kind: "flow" },
      { cmd: "HSET + PEXPIRE/PERSIST", key: "root stream keys", does: "Update <code>accessedAtNs</code> and re-arm the exact backstop once for the logical client read. Persistent and absolute-expiry-only streams skip this branch. Continuations, long-poll rechecks, inherited ranges, sealing, and repair are no-touch.", when: "first root snapshot with sliding TTL only", loc: "read.lua:60", kind: "ttl" },
      { cmd: "return meta_flat(m)", does: "Return the metadata table already loaded by this invocation. The script does not issue a second <code>HGETALL</code> to build its reply.", loc: "read.lua:159", kind: "flow" },
      { cmd: "EVALSHA + HGETALL", key: "selected segment :meta", does: "When a frame range exists, a second invocation validates that segment's incarnation and loads its metadata once. It is always no-touch. A simple non-empty root read therefore has two metadata loads total, one per script invocation, and no producer load.", loc: "store.go:732", kind: "read" },
      { cmd: "ZRANGEBYLEX", key: "selected segment :msg", does: "Fetch the first candidate with <code>LIMIT 0 1</code>. If more range exists and that frame fits, make one optional bulk call ending at <code>firstOffset + remainingTargetBytes</code>, with the remaining part of the 1,024-frame cap. There is no lookahead call.", loc: "read.lua:108", kind: "read" },
    ]}
    caption="For a fork, Go computes finite source and child segments. It runs the no-touch range invocation against each segment's own slot, capped by the fork offset and the root snapshot tail. The inherited prefix arrives first, and post-fork source appends stay invisible."
  />

  <Callout icon="info" label="Steady-state persistent command shape">
    A small non-empty read of a newly created non-expiring stream runs two
    `EVALSHA` calls, two metadata `HGETALL` calls, and one `ZRANGEBYLEX`. It
    runs zero `HSET`, `HSETNX`, `PEXPIRE`, and `PERSIST` commands. The first
    script invocation captures the root snapshot. The second reads the selected
    frame range. Producer state is absent from both.
  </Callout>

  <Complexity
    time="O(log N + M) per storage page"
    space="O(M), one bounded storage page"
    why="<code>ZRANGEBYLEX</code> descends the skiplist to the cursor in O(log N). The first call fetches one frame; an optional second call visits only end offsets inside the remaining 1 MiB budget and the remaining part of the 1,024-frame cap. One indivisible first frame may exceed the target. A fork page may seek finite segments up the chain. The handler flushes each page before asking for the next, so a long response does not require one suffix-sized storage result or a second whole-response buffer."
  />

  <Callout icon="info" label="Why the bound looks the way it does">
    The lower bound `"(<offset>\xff"` is exclusive and ends in the byte
    `0xFF`. Frames sharing the requested offset prefix have `|` (`0x7C`) as
    their next byte, which sorts below `0xFF`, so they are excluded, because
    the client already has those bytes. Any frame with a greater offset
    prefix sorts above the bound and is included. The upper bound
    `"[<snapshot-tail>\xff"` includes frames ending at the captured tail and
    excludes later appends. Payload bytes never enter the comparison. The
    fixed-width prefix decides first.[^lex]
  </Callout>
</section>

<section class="block" id="get-longpoll">
  <SectionHead num="05" label="GET" title="Read, live, long-poll" />

  `GET {stream-url}?offset=<offset>&live=long-poll` holds the request open
  until data arrives past the offset, the stream closes, or a 30-second
  timeout returns `204`.[^longpoll] The waiting runs in Go through the typed
  `WaitForPage` capability, not in a script. It subscribes before its no-touch
  recheck, so an append that lands in the attach gap is never missed.

  ```http title="Long-poll from the tail"
  GET /v1/stream/chat?offset=now&live=long-poll HTTP/1.1

  HTTP/1.1 200 OK            ← data arrived within 30 s
  Stream-Next-Offset: 0000000000000000_0000000000000033
  Stream-Cursor: 7f3a…

  HTTP/1.1 204 No Content    ← 30 s passed with nothing new
  Stream-Up-To-Date: true
  Stream-Cursor: 7f3a…
  ```

  <Diagram
    name="seq-longpoll"
    label="GET, long-poll"
    caption="The waiter never publishes. The wake is some other request's PUBLISH inside append.lua or close.lua. A 1-second defensive poll recovers any wakeup dropped by pub/sub delivery, which does not guarantee delivery."
  />

  <RedisTrace
    label="The wait loop, step by step"
    script="—  Go orchestration"
    source="store/redis/notify.go"
    steps={[
      { cmd: "ReadPage", key: "logical first read", does: "The handler reads before it subscribes. Frames return <strong>200</strong> immediately. An empty page at the tail carries the content type, closed state, incarnation, and expiry configuration needed to wait. This is the only read in the request that may renew a sliding TTL.", loc: "handler.go:513", kind: "read" },
      { cmd: "SUBSCRIBE", key: "ds:notify:{p}", does: "Open a dedicated pub/sub connection to the stream's channel and confirm it before waiting.", loc: "notify.go:108", kind: "read" },
      { cmd: "ReadPage NoTouch", key: "recheck", does: "Capture a fresh typed page before blocking and require the same stream incarnation as the first page. An append in the subscribe window is visible here. This recheck does not renew access.", loc: "notify.go:115", kind: "read" },
      { cmd: "block", does: "Wait for a channel message, a 1 s defensive poll, or the 30 s timeout. Every branch performs the same no-touch typed recheck.", loc: "notify.go:139", kind: "flow" },
      { cmd: "return ReadWaitResult", key: "authoritative final page", does: "Return the final page and timeout flag. The handler uses that page for content type, tail, closed state, next offset, and response status. It does not call <code>Store.Get</code> after the wait.", loc: "notify.go:162", kind: "flow" },
    ]}
  />

  <Callout icon="alert" label="Fork waiters never hang">
    An offset in a fork's inherited range (below `forkOffset`) can only be
    served by the source, and source appends never publish to the fork's
    channel. The handler starts a long-poll only when the effective offset is
    the root snapshot tail. An inherited-range request therefore returns its
    bounded page instead of subscribing to the fork channel.[^forkwait]
  </Callout>

  <Complexity
    time="O(log N + M) per read"
    space="O(M) returned + O(1) subscription"
    why="Every page is the same skiplist seek-and-walk. The <code>SUBSCRIBE</code> is O(1), and the block costs no compute while idle. One request does one logical first read, one no-touch attach recheck, and one no-touch recheck per wake, defensive poll, or timeout. The final page is reused for the HTTP response."
  />
</section>

<section class="block" id="get-sse">
  <SectionHead num="06" label="GET" title="Read, live, SSE" />

  `GET {stream-url}?offset=<offset>&live=sse` keeps a Server-Sent Events
  stream open and pushes each batch as it arrives.[^sse] The handler flushes
  the SSE headers, catches the client up from durable storage, then attaches it
  to the stream's shared fanout hub. Every 60 seconds it cycles the connection
  so a CDN can collapse concurrent tails.

  ```http title="SSE tail"
  GET /v1/stream/chat?offset=now&live=sse HTTP/1.1

  HTTP/1.1 200 OK
  Content-Type: text/event-stream

  event: data
  data: [{"tok":"now"}]

  event: control
  data: {"streamNextOffset":"0000000000000000_0000000000000033","streamCursor":"7f3a…"}
  ```

  <Diagram
    name="seq-sse"
    label="GET, SSE"
    caption="The data event carries the bytes. The control event after it carries the next offset and a cursor. A closed stream gets a final control with streamClosed:true, then the connection ends, and clients must not reconnect."
  />

  <RedisTrace
    label="The SSE loop, step by step"
    script="—  handler + shared hub"
    source="handler_sse.go · sse_hub.go"
    steps={[
      { cmd: "ReadPage", key: "client first page", does: "Before SSE dispatch, <code>handleRead</code> captures one typed page. It supplies content type, tail, closure, incarnation, next offset, and the first catch-up frames. No <code>Store.Get</code> or producer hash read occurs.", loc: "handler.go:513", kind: "read" },
      { cmd: "acquire hub", key: "exact stream lifetime", does: "Create or reuse one hub for this stream incarnation on the Chronicle replica. The handler passes the first page's typed snapshot into the hub.", loc: "handler_sse.go:42", kind: "flow" },
      { cmd: "SUBSCRIBE + NoTouch ReadPage", key: "ds:notify:{p}", does: "The hub confirms its persistent Redis subscription, then reads durably from the first snapshot tail without renewing access. This retains an append from the attach window before the hub reports ready.", loc: "sse_hub.go:305", kind: "read" },
      { cmd: "reuse first page", key: "client catch-up", does: "The handler emits the first page it already owns, then continuation pages under the same snapshot. It performs no extra initial bounded read and no metadata reload after catch-up.", loc: "handler_sse.go:103", kind: "read" },
      { cmd: "shared live ReadPage", key: "bounded replay", does: "After each wake, the hub performs one logical durable refresh for all local clients, formats one immutable event, and retains it in the bounded replay window. A sliding TTL renews once per hub refresh. Persistent and absolute-expiry-only streams remain read-only.", loc: "sse_hub.go:435", kind: "read" },
      { cmd: "every 60 s, return", does: "Cycle the connection so a CDN can collapse concurrent tails. The client reconnects from the last <code>streamNextOffset</code> it received.", loc: "handler_sse.go:203", kind: "flow" },
    ]}
  />

  The [SSE fanout explainer](../performance-improvements/sse-fanout) compares the old and new
  architecture, including the attach race, bounded replay policy, fault tests,
  and measured read-amplification reduction.

  <Callout icon="info" label="Binary streams">
    For a stream whose content type is neither `text/*` nor
    `application/json`, the server base64-encodes each `data` event and sets
    `stream-sse-data-encoding: base64`. The `control` events stay JSON. The
    handler picks the encoding from the stream's media type at connect
    time.[^binary]
  </Callout>

  <Complexity
    time="live reads O(S·R) · socket writes O(C)"
    space="O(S·B + C)"
    why="One hub refresh reads each active stream once on each serving Chronicle replica, then every client still receives its own socket write. <strong>S</strong> is active streams, <strong>R</strong> is serving replicas, <strong>C</strong> is clients, and <strong>B</strong> is the configured replay bound. Initial client catch-up remains one durable read path per client."
  />
</section>

<section class="block" id="head">
  <SectionHead num="07" label="HEAD" title="Read metadata" />

  `HEAD {stream-url}` returns the tail offset, content type, TTL/expiry, and
  closed status with no body.[^head] The handler captures a no-touch
  `ReadSnapshot` at `offset=now`. Redis runs the metadata branch of `read.lua`,
  so it checks lazy expiry, soft deletion, and incarnation without reading
  frames or producer state. A metadata probe is not access, so it does not
  slide the TTL.

  ```http title="Probe a stream"
  HEAD /v1/stream/chat HTTP/1.1

  HTTP/1.1 200 OK
  Content-Type: application/json
  Stream-Next-Offset: 0000000000000000_0000000000000029
  Stream-TTL: 3600
  Cache-Control: no-store
  ```

  <Diagram
    name="seq-head"
    label="HEAD, metadata"
    caption="One typed no-touch snapshot supplies every response header. Cache-Control: no-store keeps a stale tail offset or closed flag from being cached."
  />

  <RedisTrace
    script="read.lua, metadata branch"
    source="handler_stream.go · store/redis/scripts/read.lua"
    steps={[
      { cmd: "ReadPage offset=now", key: "TargetBytes 1 · MaxFrames 1 · NoTouch", does: "Ask the page reader for a typed snapshot only. Resolving <code>now</code> to the captured tail leaves no frame range to fetch.", loc: "handler_stream.go:37", kind: "read" },
      { cmd: "EVALSHA + HGETALL", key: "ds:{p}:meta", does: "Load metadata once. The <code>tail</code> field is the existence marker. The reply contains the same metadata map, so no second <code>HGETALL</code> runs.", loc: "read.lua:32", kind: "read" },
      { cmd: "classify", does: "Expired → lazy cleanup and <strong>404</strong>. Soft-deleted → <strong>410</strong>. A legacy stream without an incarnation runs one <code>HSETNX</code> migration. A current live stream performs no write.", loc: "read.lua:35", kind: "flow" },
      { cmd: "skip :prod · skip :msg", does: "HEAD needs neither producer fencing state nor frames. <code>NoTouch</code> also skips <code>accessedAtNs</code> and the TTL backstop.", loc: "store.go:691", kind: "flow" },
    ]}
  />

  <Complexity
    time="O(1)"
    space="O(1), no body"
    why="HEAD reads the bounded metadata hash once and returns a typed snapshot. It does not read the producer hash or frame set. A current live stream is not mutated, and no body crosses the wire. Lazy expiry cleanup and one-time legacy incarnation migration remain exceptional writes."
  />
</section>

<section class="block" id="delete">
  <SectionHead num="08" label="DELETE" title="Delete a stream" />

  `DELETE {stream-url}` removes a stream and its data. If forks still
  reference it, the stream cannot be removed, because its data is needed for
  their inherited prefix. So `delete.lua` soft-deletes instead. The URL
  answers `410` to clients while the data stays for fork reads.[^delete] When
  the last fork detaches, garbage collection cascades up the fork chain.

  ```http title="Delete a fork"
  DELETE /v1/stream/branch HTTP/1.1

  HTTP/1.1 204 No Content
  ```

  <Diagram
    name="seq-delete"
    label="DELETE, with fork cascade"
    caption="If the parent was soft-deleted and its refcount just reached zero, decr_ref.lua hard-deletes it and returns CASCADE with the grandparent's path, and Go walks one step further up."
  />

  <RedisTrace
    script="delete.lua"
    source="store/redis/scripts/delete.lua"
    steps={[
      { cmd: "HGETALL", key: "ds:{p}:meta", does: "Load meta. Absent → <code>NOTFOUND</code> (<strong>404</strong>). Delete has no expiry check. An expired-but-present stream is just deleted.", loc: "delete.lua:12", kind: "read" },
      { cmd: "softDel?", does: "Already soft-deleted → <code>SOFTDEL</code> (<strong>410</strong>), consistent with every other direct operation on a soft-deleted URL.", loc: "delete.lua:18", kind: "flow" },
      { cmd: "refCount > 0?", does: "If forks still reference this stream, take the soft path below instead of deleting.", loc: "delete.lua:20", kind: "flow" },
      { cmd: "DEL", key: "ds:{p}:meta · :msg · :prod · :forks", does: "The hard path: remove every key for the stream in one call.", loc: "delete.lua:30", kind: "write" },
      { cmd: "PUBLISH", key: "ds:notify:{p}", does: "Send <code>d</code>. Any waiter wakes, re-reads, finds the stream gone, and its request ends.", loc: "delete.lua:31", kind: "pub" },
    ]}
    caption="The soft path instead runs HSET :meta softDel 1 and PERSIST on all four keys. It keeps the data and drops the TTL backstop so a fork can keep reading the inherited prefix."
  />

  <RedisTrace
    label="The cascade, one ancestor at a time"
    script="decr_ref.lua"
    source="store/redis/scripts/decr_ref.lua"
    steps={[
      { cmd: "HGETALL", key: "parent:meta", does: "If the source already vanished → <code>GONE</code>, nothing to do.", loc: "decr_ref.lua:16", kind: "read" },
      { cmd: "HINCRBY", key: "parent:meta refCount -1", does: "Release this fork's reference on the source.", loc: "decr_ref.lua:19", kind: "write" },
      { cmd: "SREM", key: "parent:forks /v1/stream/branch", does: "Unregister the child from the source's fork set.", loc: "decr_ref.lua:20", kind: "write" },
      { cmd: "refCount == 0 & softDel?", does: "The last fork just detached from a soft-deleted source: cascade.", loc: "decr_ref.lua:27", kind: "flow" },
      { cmd: "DEL · PUBLISH", key: "parent:*", does: "Hard-delete the source and return <code>CASCADE</code> with <em>its</em> <code>forkedFrom</code>, so Go runs <code>decr_ref.lua</code> one step further up the chain.", loc: "decr_ref.lua:30", kind: "write" },
    ]}
  />

  <Callout icon="alert" label="The cascade is not atomic across slots">
    `delete.lua` and each `decr_ref.lua` are individually atomic, but they
    run against different streams in different cluster slots, joined by Go
    (`releaseRef`). A crash mid-cascade leaves a parent's refcount one too
    high until another delete re-walks the chain. This is a documented
    window, the same trade made by fork creation.[^cascade]
  </Callout>

  <Complexity
    time="O(M) hard delete · O(1) soft delete"
    space="frees O(M)"
    why="A hard delete frees the whole <code>:msg</code> skiplist, which is O(M) for M frames, plus the bounded hashes, because <code>DEL</code> of a collection is linear in its size. A soft delete (forks still reference it) only sets a flag and drops TTLs, so it is O(1). The cascade runs one O(1) <code>decr_ref</code> per ancestor and frees each hard-deleted ancestor's frames, so a chain of depth d costs O(d) scripts plus the frames reclaimed."
  />

</section>

<section class="block" id="further">
  <SectionHead num="09" label="Further reading" title="Where this <em>comes from</em>" />

  Every command above is taken verbatim from the source. The locators are
  `file:line` into the repository at the time of writing.

  - [Overview](../). The data model, design, performance, and conformance posture.
  - [store/redis/scripts/](https://github.com/adityavkk/chronicle/tree/main/store/redis/scripts). Seven operation scripts plus their shared validation and reply helpers. `read.lua` returns the typed atomic read projection; the other six perform mutations.
  - [docs/spec/PROTOCOL.md](https://github.com/adityavkk/chronicle/blob/main/docs/spec/PROTOCOL.md). The vendored protocol specification.
  - [docs/PLAN.md](https://github.com/adityavkk/chronicle/blob/main/docs/PLAN.md). The Redis data model and its decision log.
</section>

[^offset]: Offset format and parsing: [`store/offset.go`](https://github.com/adityavkk/chronicle/blob/main/store/offset.go). The format is `"%016d_%016d"`, lexicographically sortable, with `-1` → start and `now` → tail.
[^frame]: Frame encoding: [`store/redis/keys.go`](https://github.com/adityavkk/chronicle/blob/main/store/redis/keys.go). The `encodeFrame` function writes the `"<offset>|<data>"` ZSET member at score `0`.
[^reply]: The fixed nine-field reply and its decoding: [`store/redis/scripts.go`](https://github.com/adityavkk/chronicle/blob/main/store/redis/scripts.go), `scriptReply` / `make_reply` in [`common.lua`](https://github.com/adityavkk/chronicle/blob/main/store/redis/scripts/common.lua).
[^zset]: Redis sorted-set complexity: [`ZADD`](https://redis.io/docs/latest/commands/zadd/) is `O(log(N))` per member, and [`ZRANGEBYLEX`](https://redis.io/docs/latest/commands/zrangebylex/) is `O(log(N) + M)`. `ZRANGEBYLEX` is only well-defined when every member shares one score, and here all scores are `0`. Small sets use a listpack (`zset-max-listpack-entries`, default 128) and convert to the skiplist past it.
[^create]: Create semantics and idempotent-match rules: PROTOCOL.md §5.1.
[^fork]: Stream forking, fork offsets, and the soft-delete lifecycle: PROTOCOL.md §4.2.
[^append]: Append, JSON flattening, and `Stream-Seq`: PROTOCOL.md §5.2; JSON processing in [`store/json.go`](https://github.com/adityavkk/chronicle/blob/main/store/json.go).
[^producer]: Idempotent producers (the epoch/sequence state machine): PROTOCOL.md §5.2.1; `validate_producer` in [`common.lua`](https://github.com/adityavkk/chronicle/blob/main/store/redis/scripts/common.lua).
[^close]: Stream closure, durable, monotonic, and idempotent: PROTOCOL.md §4.1 and §5.3.
[^read]: Catch-up reads, `Stream-Up-To-Date`, and EOF signaling: PROTOCOL.md §5.6.
[^lex]: The exclusive lower bound and why payload bytes never enter the comparison: `lexLowerBound` in [`store/redis/keys.go`](https://github.com/adityavkk/chronicle/blob/main/store/redis/keys.go).
[^longpoll]: Long-poll mode, timeout, and closed-stream behavior: PROTOCOL.md §5.7; the wake protocol in [`store/redis/notify.go`](https://github.com/adityavkk/chronicle/blob/main/store/redis/notify.go).
[^forkwait]: The long-poll tail guard in [`handler.go`](https://github.com/adityavkk/chronicle/blob/main/handler.go), followed by typed race closure in [`store/redis/notify.go`](https://github.com/adityavkk/chronicle/blob/main/store/redis/notify.go).
[^sse]: SSE mode, the data/control event format, and connection cycling: PROTOCOL.md §5.8; [`handler_sse.go`](https://github.com/adityavkk/chronicle/blob/main/handler_sse.go).
[^binary]: Base64 encoding for binary SSE streams: PROTOCOL.md §5.8; the media-type check in [`handler.go`](https://github.com/adityavkk/chronicle/blob/main/handler.go).
[^head]: Stream metadata via `HEAD`, and the `no-store` caching guidance: PROTOCOL.md §5.5.
[^delete]: Delete, soft-delete, and cascading garbage collection: PROTOCOL.md §5.4 and §4.2.
[^cascade]: The cross-slot cascade and its documented crash window: `Delete` / `releaseRef` in [`store/redis/store.go`](https://github.com/adityavkk/chronicle/blob/main/store/redis/store.go).
