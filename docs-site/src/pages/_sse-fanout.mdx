---
title: "Read each live update once, <em>then share it with every client</em>"
summary: "The old system read the same Redis data and opened a notification connection once per client. The new system shares one durable reader per active stream and one store-owned Pub/Sub multiplexer while preserving exact resume offsets."
eyebrow: "Architecture"
meta: "Performance improvements · SSE fanout"
deck: "Browsers keep separate SSE connections and resume offsets. Inside Chronicle, one stream hub reads and formats each update once, an indexed ring retains bounded replay, and one store-owned multiplexer carries logical stream registrations over a bounded number of Redis Pub/Sub connections."
byline:
  - label: "Live reads"
    value: "970.5 → 1.000 per append"
  - label: "One-stream delivery"
    value: "49,099 events/s"
  - label: "Notification topology"
    value: "100 logical · 1 physical"
  - label: "Safety"
    value: "332/332 + Jepsen"
---

import Tldr from "../components/Tldr.astro";
import SectionHead from "../components/SectionHead.astro";
import Callout from "../components/Callout.astro";
import Table from "../components/Table.astro";
import Diagram from "../components/Diagram.astro";
import Aside from "../components/Aside.astro";
import Complexity from "../components/Complexity.astro";
import SSEAmplification from "../components/SSEAmplification.astro";
import SSEResults from "../components/SSEResults.astro";
import SSEUserJourney from "../components/SSEUserJourney.astro";
import SimpleBeforeAfter from "../components/SimpleBeforeAfter.astro";

<SimpleBeforeAfter
  id="simple-explanation"
  title="One update no longer causes the same Redis read for every browser"
  intro="Each browser still has its own connection. Chronicle now shares the storage read and event formatting that those connections need."
  clientAction="Keep one SSE connection open and save the last durable offset."
  oldLabel="Every browser reads again"
  oldSummary="A Redis wake hint caused every client handler to fetch and format the same durable update."
  oldSteps={[
    { actor: "Producer", action: "Appends one durable update to Redis." },
    { actor: "Redis", action: "Publishes a short wake hint." },
    { actor: "Every client handler", action: "Wakes and reads the same stream data from Redis." },
    { actor: "Every client handler", action: "Formats and writes its own copy of the same event." },
  ]}
  newLabel="One hub reads for everyone"
  newSummary="One hub for the active stream reads and formats the update. Local client handlers reuse it."
  newSteps={[
    { actor: "Producer", action: "Appends the same durable update to Redis." },
    { actor: "Redis", action: "Wakes one stream hub on this Chronicle replica." },
    { actor: "Stream hub", action: "Reads the durable update once and formats one data event." },
    { actor: "Client handlers", action: "Reuse that event and write it to their separate browser connections." },
  ]}
  changed="The Redis read and data-event formatting happen once per active stream on each serving replica."
  unchanged="Every browser keeps its own HTTP connection, durable offset, control event, and socket write."
  downside="Chronicle keeps a bounded replay window for each active stream. A slow browser must disconnect and resume when it falls behind."
/>

<Tldr>
  <ul>
    <li><strong>Before:</strong> 1,000 clients could cause about 1,000 Redis reads for one append.</li>
    <li><strong>Now:</strong> one hub reads the stream once and shares the formatted event with local clients. One store-owned multiplexer carries every logical stream registration over one physical Pub/Sub connection by default.</li>
    <li><strong>Result:</strong> the measured ratio fell from 970.5 Redis reads per append to 1.000.</li>
    <li><strong>Attach:</strong> registration precedes the authoritative first page. The first generation has no readiness reread, but one final bounded no-touch incarnation page still fences replacement immediately before indexed attach.</li>
    <li><strong>Failure:</strong> if replacement is discovered after the <code>200</code> is committed, Chronicle aborts the transport. It sends no replacement data and no ordinary HTTP error body.</li>
    <li><strong>Cost:</strong> socket writes still happen once per client, and slow clients may have to reconnect from their last saved offset.</li>
  </ul>
</Tldr>

<Callout icon="arrow" label="Performance improvements">
  The [performance improvements index](../) links this shared-read mechanism to
  the scaling report, cloud load tests, configuration comparison, architecture
  decision, and tracking issue.
</Callout>

<section class="block" id="old-path">
  <SectionHead num="01" label="Before" title="One append caused <em>the same work N times</em>" />

  The old handler gave every SSE client its own read and wait loop. When an
  append landed, Redis published a short wake message. Every waiting client
  woke, reread the same durable suffix, fetched stream metadata, formatted the
  same SSE data event, and wrote it to its own socket.

  The wake message did not contain the stream data, which was correct. Redis
  Pub/Sub can lose a message during a disconnect, so clients had to read the
  durable stream after every wake. Each client performed that durable read even
  though every client on the stream needed the same bytes.

  <Diagram
    name="wiki-sse-old"
    label="Before · one loop per client"
    caption="<strong>Every blue return path is duplicate durable work.</strong> The clients need separate socket writes, but they do not need separate Redis reads or separate copies of the same formatted data event."
  />

  <Table>
    | Sealed workload | Redis publishes | Live `ZRANGEBYLEX` reads | Reads per append |
    | --- | ---: | ---: | ---: |
    | 1 stream, 1,000 clients | 863 | 837,579 | **970.5** |
    | 100 streams, 2,048 clients | 26,900 | 539,956 | **20.1** |
  </Table>

  The difference between 1,000 clients and 970.5 reads per append comes from
  timing and incomplete delivery during the measured window. More clients on
  one stream still created more duplicate reads.
  Increasing Chronicle and Redis from 4 to 16 vCPU did not move the observed
  SSE ceiling beyond roughly 25,000 to 32,000 delivered events per second.[^adr]
</section>

<section class="block" id="new-path">
  <SectionHead num="02" label="After" title="One stream hub does <em>the shared work once</em>" />

  The new handler creates or reuses one hub for the stream on the current
  Chronicle replica. The hub owns one logical notification registration. A
  store-owned multiplexer carries all such registrations over one physical
  Redis Pub/Sub connection by default. After a wake hint, the hub pages through
  one exact durable tail, formats one immutable SSE data event, and places that
  event in a bounded replay window. Each local client has only a coalesced wake
  signal and its own durable offset.

  <Diagram
    name="wiki-sse-new"
    label="After · one hub per active stream and replica"
    caption="<strong>The live Redis path is shared.</strong> Socket writes remain per client because each HTTP connection is real. The data read and data-event formatting happen once."
  />

  <Table>
    | Work | Old setup | New setup |
    | --- | --- | --- |
    | Logical notification registration | one per SSE client | one per active stream on each serving replica |
    | Physical Redis Pub/Sub connection | one per SSE client | one per store by default |
    | Durable live read after an append | one per client | one per stream hub |
    | Data-event formatting and base64 | repeated by every client | one shared immutable event |
    | Client progress lookup | linear ring scan | monotonic sequence plus indexed boundary lookup |
    | Slow client | could keep its loop and socket blocked | disconnects after replay lag or write timeout |
    | Last client leaves | its loop closes | the hub and logical registration close |
  </Table>

  <Complexity
    time="reads: O(S·R), writes: O(C)"
    space="O(S·B + C)"
    why="<strong>S</strong> is the number of active streams, <strong>R</strong> is the number of Chronicle replicas that currently serve clients for those streams, <strong>C</strong> is the client count, and <strong>B</strong> is the bounded replay size. Redis reads now follow streams and serving replicas. Socket writes still follow clients."
  />

  <SSEAmplification />
</section>

<section class="block" id="user-journey">
  <SectionHead num="03" label="Walkthrough" title="Follow one user from <em>connect to resume</em>" />

  Rina watches order updates on a store dashboard. Luis and a wallboard watch
  the same stream. The walkthrough follows one append through all three
  connections, then shows what happens when the wallboard stalls.

  <SSEUserJourney />
</section>

<section class="block" id="handoff">
  <SectionHead num="04" label="Safety" title="The hard part is <em>joining without a gap</em>" />

  Sharing the live path creates a handoff problem. A client may arrive while an
  append, delete, or recreation is in flight. Chronicle confirms the hub's
  logical notification registration before storage captures the request's
  authoritative first page. It then carries that page to the client and the hub
  instead of rereading the same offset for readiness.

  <Diagram
    name="seq-sse-hub-attach"
    label="Catch-up to live handoff"
    caption="The multiplexer confirms the hub's logical registration before the first catch-up page captures its tail. The handler confirms the stream incarnation and attaches at the exact control offset it flushed. An append can land at any point without being lost or sent before its control offset."
  />

  Nine rules protect this boundary:

  1. **Register before the first page.** The multiplexer waits for the current
     Redis connection generation to acknowledge the hub's logical channel. The
     first `ReadPage` then captures the incarnation and exact tail.
  2. **Seed readiness from page one.** Client catch-up uses `ReadPage` and the
     first confirmed hub generation starts from that authoritative snapshot.
     It does not reread the same offset for readiness. Catch-up stops at its
     captured tail even when appends continue, and neither catch-up nor hub
     refresh can fall back to an unbounded `Store.Read`.
  3. **Flush before advancing.** The handler puts the page's data and control
     flush under one write deadline. It advances the attach offset only after
     that control value reaches the client.
  4. **Confirm the lifetime immediately before attach.** The handler reads one
     bounded no-touch identity page after catch-up. This is not an initial
     readiness refresh. It is the final fence against a path deleted and
     recreated after the authoritative page returned.
  5. **Attach at the exact boundary.** The handler uses the ring's sequence and
     boundary index to find the exact controlled offset. If eviction removed it,
     Chronicle closes the connection and lets the client resume. It never skips
     to a newer offset.
  6. **Recheck the durable stream.** A wake is only a hint. The hub reads Redis
     after a hint and once per second while idle. A lost Pub/Sub message delays
     delivery by at most one poll. It does not lose data.
  7. **Recover the notification connection.** A reconnect invalidates old
     acknowledgements, restores all desired channels, and wakes each hub for an
     immediate durable no-touch refresh. A replacement subscription generation
     refreshes the same way before waiting. Append and close hints and defensive
     polls retain their access-touch semantics. A coalesced nonblocking signal
     keeps one blocked hub from holding another.
  8. **Name each stream lifetime.** Every create stores a random 128-bit
     incarnation ID. If a path is deleted and recreated, the old hub stops
     instead of serving the new stream with old state.
  9. **Bound slow consumers.** The replay window is 1 MiB by default and each
     client flush has a ten-second deadline. A lagging client disconnects and
     resumes from the last control offset it actually received.

  Redis orders an append either before or after the registered first page. An
  append before the page is inside its fixed catch-up tail. An append after the
  page is covered by the confirmed subscription. The safety poll recovers a
  lost notification, and the incarnation check fences a delete and recreation.
  The [SSE endpoint sequence](../../endpoints#get-sse) follows the same request
  from authorization through the final data and control events.

  <Table wide>
    | Durable read cause | Read count at that point | `NoTouch` | Purpose |
    | --- | ---: | --- | --- |
    | Authoritative first page | one | no | Capture content type, exact tail, closure, expiry, and incarnation after registration acknowledgement |
    | First-generation readiness | zero additional reads | n/a | Seed hub readiness from the authoritative page without repeating its offset |
    | Final attach confirmation | one | yes | Confirm the same incarnation immediately before live ring lookup |
    | Append or close hint | bounded pages to one captured tail | no | Deliver new durable data and preserve active-reader sliding TTL semantics |
    | Defensive poll | bounded pages to one captured tail | no | Recover a dropped hint and preserve active-reader sliding TTL semantics |
    | Reconnect event or replacement generation | immediate bounded pages | yes | Recover a notification gap without extending stream lifetime |
  </Table>

  A replacement can occur after the first page has already committed the SSE
  response. A non-200 status is then physically impossible. The final
  confirmation still prevents replacement data and prevents live attach. The
  handler converts that post-commit failure to `http.ErrAbortHandler`, so
  `net/http` terminates the stream and does not append a text error body. A
  browser resumes from the last control offset it received.

  Numeric long-poll waits use the same store-owned multiplexer. The Redis
  `PageWaiter` registers through `SubscribeNotifications`, performs its
  compatibility recheck, and returns the durable wake or timeout page. The HTTP
  handler uses that returned page directly instead of reading the same offset
  again. The optional `PageWaiter` and `NotificationSubscriber` capabilities
  remain independent interfaces for third-party stores.

  <Callout icon="alert" label="Correctness gate">
    The optimization is allowed to change how work is shared. It is not allowed
    to change Durable Streams behavior. The complete conformance suite remains
    a merge blocker. The Jepsen and Porcupine suites remain maintained blockers.
    Producer fencing, append atomicity, closure, expiry, forks, offsets, and
    persistence must stay correct even if a faster design is available.
  </Callout>
</section>

<section class="block" id="results">
  <SectionHead num="05" label="Results" title="Read amplification collapsed, then <em>sockets became visible</em>" />

  <SSEResults />

  The shared-hub image was tested with one Chronicle replica, one Redis primary, and
  AOF configured to sync every write. Chronicle had 2 vCPU and 4 GiB. Redis had
  2 vCPU and 12 GiB. The one-stream workload used 1,000 clients for 20 seconds.
  The 100-stream workload used 2,048 clients for 15 seconds. Each workload ran
  three times from fresh result state. The table uses the component median.[^shared-hub]

  <Table wide>
    | Workload | Result | Delivered | Delivery rate | p50 | p99 | Completion | Errors |
    | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
    | 1 stream, 1,000 clients | per-client baseline | 992,320 | 49,616/s | 7.123 ms | 19.519 ms | 99.232% | 0 |
    | 1 stream, 1,000 clients | shared-hub median | 981,983 | **49,099/s** | 5.047 ms | **8.591 ms** | 98.198% | 0 |
    | 100 streams, 2,048 clients | per-client baseline | 1,173,067 | 78,204/s | 40.159 ms | 71.167 ms | 76.371% | 0 |
    | 100 streams, 2,048 clients | shared-hub median | 1,495,527 | **99,702/s** | 23.967 ms | **51.903 ms** | 97.365% | 0 |
  </Table>

  The exact-counter guard opened 1,000 clients and waited for catch-up reads to
  settle. Redis `PUBLISH` then advanced from 3,480 to 3,580, while
  `ZRANGEBYLEX` advanced from 5,602 to 5,702. The instrumented hub also
  completed 100 `ReadPage` calls. The result was 1.000 Redis read command and
  1.000 durable page per append, which passes the 1.2 gate.[^shared-hub]

  The fused root-read follow-up supplies one authoritative first page after
  registration. The first confirmed hub generation becomes ready from that
  page with no duplicate initial refresh. The shared-hub evidence retains one final bounded
  no-touch identity confirmation immediately before indexed attach. It fences
  a delete/recreate race without delivering client data twice at the same
  offset. Reconnect generations also refresh durable state immediately with
  no access touch.

  Every final workload repetition recorded zero backpressure and zero other
  client errors. Median peak sampled Chronicle RSS was 134.25 MiB for one stream
  and 396.89 MiB for 100 streams. The 100-stream run used 100 logical
  notification registrations over one physical Redis Pub/Sub connection.

  The frame benchmark also changed the per-client control path. A typed reusable
  buffer reduced allocation from 905 to 216 bytes per update, a 76.1 percent
  reduction, while median time improved by 4.8 percent. A focused CPU profile
  then removed repeated Redis path-escaper construction. The 100-stream median
  moved from a failing 75.647 ms p99 to 51.903 ms.[^shared-hub]

  <Aside label="How to read the throughput number" summary="The read ratio is the direct proof; the rate comparison is directional">
    The per-client baseline and shared-hub image used the same local Kubernetes
    workload shape and resource split, but they were not randomized on an
    untouched cluster. The exact page and Redis command counters prove the
    shared-read mechanism. The three-run medians reduce host noise without
    turning the throughput comparison into a universal capacity claim.
  </Aside>

  The full 332-test conformance suite passed. Four live Jepsen scenarios passed:
  baseline, origin restart, Redis restart, and SSE resume. The SSE scenario delivered 320
  expected events exactly once across eight checked readers and verified 11
  fault actions, including replay lag, write timeout, delete and recreate, and
  attachment at a page boundary. A direct Redis oracle checked the final frames,
  offsets, tail, closed state, and producer state.[^jepsen]
</section>

<section class="block" id="operations">
  <SectionHead num="06" label="Operations" title="Observe the shared path <em>and prove cleanup</em>" />

  The metrics separate logical demand from physical Redis connections. They
  also account for every byte retained by the indexed replay ring. No SSE
  metric uses a stream path label.

  <Table wide>
    | Question | Metric | Expected signal |
    | --- | --- | --- |
    | How many streams have a local live reader? | `chronicle_sse_hubs` | One hub per active stream and Chronicle replica, then zero after the last lease closes |
    | How many clients use those hubs? | `chronicle_sse_clients` | One per active SSE response, independent of Redis registration count |
    | How many logical channels are registered? | `chronicle_sse_subscriptions` | One per active stream by default |
    | How many physical Redis connections carry them? | `chronicle_sse_notification_connections{topology}` | One for the default single-group topology, then zero after the last logical registration closes |
    | Why did a hub read durable state? | `chronicle_sse_hub_refreshes_total{cause}` | Separate `hint`, `poll`, `reconnect`, `initial`, and `attach` refreshes |
    | How much replay memory is retained? | `chronicle_sse_hub_ring_raw_bytes`, `chronicle_sse_hub_ring_wire_bytes`, `chronicle_sse_hub_ring_index_bytes` | Components sum exactly to `chronicle_sse_hub_ring_bytes` |
    | Did exact lookup fail? | `chronicle_sse_watcher_lookup_misses_total` | A miss disconnects the client; it never advances to a newer boundary |
    | Did a client exceed a bound? | `chronicle_sse_lagged_disconnects_total`, `chronicle_sse_write_timeouts_total` | Cumulative reasons for resume, not queued payload growth |
  </Table>

  The 30-minute active fault run kept 20 writers, 40 regular SSE clients, and
  one repeatedly throttled client active. The writer rate was one 256-byte
  message per stream per second.[^shared-hub]

  <Table wide>
    | Signal | Measured result |
    | --- | ---: |
    | Successful appends | 36,000 |
    | SSE deliveries | 72,040 |
    | Protocol reconnects | 69,879 |
    | Load generator errors | 0 |
    | Chronicle RSS | 48.2 MiB mean; 56.5 MiB peak |
    | Sampled heap | 20.1 MiB allocated peak; 27.7 MiB in-use peak |
    | Sampled goroutines and file descriptors | 157 goroutines peak; 143 descriptors peak |
    | Notification topology | 20 to 21 logical registrations; 1 physical connection |
    | Replay ring | 3,138,828 bytes peak, including 250,272 index bytes |
    | Bound events | 0 lag disconnects; 244 write timeouts caused by the throttled fault client |
  </Table>

  <Table wide>
    | Resource after every client left | Final value |
    | --- | ---: |
    | SSE clients | 0 |
    | Stream hubs | 0 |
    | Logical notification registrations | 0 |
    | Physical notification connections | 0 |
    | Raw replay bytes | 0 |
    | Formatted wire bytes | 0 |
    | Boundary-index bytes | 0 |
    | Total replay-ring bytes | 0 |
  </Table>

  The cleanup goroutine profile contained eight goroutines. Task images,
  binaries, Kind and Colima resources, and build caches were removed after the
  evidence archive was sealed. The evidence directory retains result records,
  counters, profiles, configuration, metrics, a manifest, and checksums. It
  does not retain runnable infrastructure.
</section>

<section class="block" id="limits">
  <SectionHead num="07" label="Limits" title="What this fix does <em>not</em> remove" />

  The hub removes duplicate live reads. It does not make every SSE cost shared.

  - **Initial catch-up still reads once per client.** A client that starts far
    behind the tail still replays its own history from Redis.
  - **Socket work still scales with clients.** Each connection needs its own
    control event, write, flush, deadline, and kernel socket buffer.
  - **Subscriptions still scale with streams and replicas.** One active stream
    on three serving Chronicle replicas uses three Redis subscriptions.
  - **The safety poll reads idle streams once per second.** This recovers lost
    wake hints and refreshes sliding TTL, but it becomes visible at a high
    active-stream count.
  - **The replay window adds shared state.** It is bounded, but the attach,
    eviction, and delete-recreate paths are more complex than one isolated loop.

  CPU profiles now point mostly at socket syscalls. Remaining allocation work
  includes per-client control framing and Redis connection buffers. Initial
  historical replay also remains per client. Those are separate improvements.
  None should weaken the conformance or Jepsen gates.

  The [operations section](#operations) gives the complete 30-minute resource
  inventory and the zeroed cleanup gauges.

  <Callout icon="info" label="Validation boundary">
    The results on this page are local Kubernetes and host validation results.
    They do not claim a paid GCP replay. The Chronicle image digest was
    <code>sha256:d4cd1389…</code> (the task image was removed after evidence capture).
  </Callout>

  <Callout icon="arrow" label="Related performance work">
    Return to [Performance improvements](../) for the horizontal scaling report,
    load tests, and Chronicle, Ursula, Rust, and Node configuration comparison.
    Read the [registered first-page decision](https://github.com/adityavkk/chronicle/blob/main/docs/adr/0007-fuse-root-pages-and-register-live-reads-first.md)
    and track its follow-up in
    [GitHub issue #172](https://github.com/adityavkk/chronicle/issues/172).
    The [SSE fanout decision][adr] records the shared-hub design and its
    remaining limits.
  </Callout>
</section>

[^adr]: [ADR-0005, Share live SSE reads through per-instance stream hubs](https://github.com/adityavkk/chronicle/blob/main/docs/adr/0005-per-instance-sse-stream-fanout.md). It records the sealed before measurements, the decision, safety rules, and remaining limits.
[^shared-hub]: The retained [shared-hub evidence](https://github.com/adityavkk/chronicle/blob/main/docs/benchmarks/sse-fanout/20260731-shared-hub-local/README.md) includes all six final client JSON records, Redis and page counters, active and cleanup runtime samples, frame benchmarks, focused profiles, the 30-minute fault run, a manifest, and checksums.
[^jepsen]: The [`sse-resume` scenario](https://github.com/adityavkk/chronicle/blob/main/jepsen/README.md) checks attach, resume, dropped and duplicate notifications, Redis and Chronicle replacement, bounded replay lag, write timeout, close ordering, and an independent durable-store oracle.
