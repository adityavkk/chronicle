<figure class="results">
  <div class="result-label">Measured local reader curve</div>

  <div class="stats">
    <div class="stat">
      <strong>435</strong>
      <span>measured complete bodies at the 128-reader cap</span>
    </div>
    <div class="stat">
      <strong>0</strong>
      <span>server errors at 512 readers</span>
    </div>
    <div class="stat">
      <strong>1,305.3 MiB</strong>
      <span>Chronicle RSS at 512 readers</span>
    </div>
    <div class="stat">
      <strong>4 KiB</strong>
      <span>largest socket write in the 16 MiB benchmark</span>
    </div>
  </div>

  <div class="curve">
    <div class="curve-head">
      <div>
        <span>Chronicle resident memory</span>
        <strong>Same local host except the sealed 512-reader baseline</strong>
      </div>
      <span class="scale">0 to 1,305 MiB</span>
    </div>

    <div class="reader-row">
      <div class="reader">
        <strong>8</strong>
        <span>readers</span>
      </div>
      <div class="pairs">
        <div class="bar before">
          <span>before</span>
          <i><b style="width:34%"></b></i>
          <strong>436.3</strong>
        </div>
        <div class="bar after">
          <span>after</span>
          <i><b style="width:5.3%"></b></i>
          <strong>69.5</strong>
        </div>
      </div>
      <span class="verdict">0 errors</span>
    </div>

    <div class="reader-row">
      <div class="reader">
        <strong>32</strong>
        <span>readers</span>
      </div>
      <div class="pairs">
        <div class="bar before">
          <span>before</span>
          <i><b style="width:61.8%"></b></i>
          <strong>793.5</strong>
        </div>
        <div class="bar after">
          <span>after</span>
          <i><b style="width:15%"></b></i>
          <strong>195.9</strong>
        </div>
      </div>
      <span class="verdict">0 errors</span>
    </div>

    <div class="reader-row critical">
      <div class="reader">
        <strong>128</strong>
        <span>readers</span>
      </div>
      <div class="pairs">
        <div class="bar before">
          <span>before</span>
          <i><b style="width:56.4%"></b></i>
          <strong>723.7</strong>
        </div>
        <div class="bar after">
          <span>after</span>
          <i><b style="width:36.1%"></b></i>
          <strong>470.7</strong>
        </div>
      </div>
      <span class="verdict changed">128 errors → 0</span>
    </div>

    <div class="reader-row">
      <div class="reader">
        <strong>512</strong>
        <span>readers</span>
      </div>
      <div class="pairs">
        <div class="bar unavailable">
          <span>before</span>
          <i><b></b></i>
          <strong>n/a</strong>
        </div>
        <div class="bar after">
          <span>after</span>
          <i><b style="width:100%"></b></i>
          <strong>1,305</strong>
        </div>
      </div>
      <span class="verdict">0 errors</span>
    </div>
  </div>

  <figcaption>
    Memory values are MiB. The old 128-reader process failed every accepted
    request after Redis read timeouts, so its lower memory use is not a passing
    result. The sealed 512-reader baseline used different hardware and is
    excluded from the memory chart.
  </figcaption>
</figure>

<style lang="scss">
  .results {
    margin: 32px 0 30px var(--rail-indent);
    max-width: var(--prose-max);
    overflow: hidden;
    border: 1px solid var(--rule);
    border-radius: var(--radius);
    background: var(--card);
    box-shadow: var(--shadow-sm);
  }
  .result-label {
    padding: 14px 24px;
    border-bottom: 1px solid var(--rule);
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--accent);
  }
  .stats {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    border-bottom: 1px solid var(--rule);
  }
  .stat {
    min-width: 0;
    padding: 18px 20px;
    border-right: 1px solid var(--rule-soft);
  }
  .stat:last-child { border-right: 0; }
  .stat strong {
    display: block;
    color: var(--accent-deep);
    font: 600 18px/1.2 var(--mono);
  }
  .stat span {
    display: block;
    margin-top: 7px;
    color: var(--ink-mute);
    font-size: 11.5px;
    line-height: 1.4;
  }
  .curve {
    padding: 22px 24px 25px;
  }
  .curve-head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 8px;
    padding-bottom: 13px;
    border-bottom: 1px solid var(--rule);
  }
  .curve-head span,
  .curve-head strong {
    display: block;
  }
  .curve-head div > span {
    font: 9.5px var(--mono);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--accent);
  }
  .curve-head div > strong {
    margin-top: 4px;
    font-size: 12.5px;
  }
  .curve-head .scale {
    font: 9.5px var(--mono);
    color: var(--muted);
  }
  .reader-row {
    display: grid;
    grid-template-columns: 58px minmax(0, 1fr) 104px;
    gap: 15px;
    align-items: center;
    padding: 14px 0;
    border-bottom: 1px solid var(--rule-soft);
  }
  .reader-row:last-child { border-bottom: 0; padding-bottom: 0; }
  .reader-row.critical {
    margin: 0 -10px;
    padding-inline: 10px;
    background: linear-gradient(90deg, transparent, var(--accent-tint), transparent);
  }
  .reader strong,
  .reader span {
    display: block;
  }
  .reader strong {
    font: 600 17px var(--mono);
    color: var(--ink);
  }
  .reader span {
    font-size: 9px;
    color: var(--muted);
  }
  .pairs {
    display: grid;
    gap: 7px;
  }
  .bar {
    display: grid;
    grid-template-columns: 42px minmax(40px, 1fr) 40px;
    gap: 8px;
    align-items: center;
    font: 9px var(--mono);
    color: var(--ink-mute);
  }
  .bar > i {
    height: 7px;
    overflow: hidden;
    border-radius: 999px;
    background: var(--paper-warm);
  }
  .bar b {
    display: block;
    min-width: 3px;
    height: 100%;
    border-radius: inherit;
    background: color-mix(in srgb, var(--bad) 70%, var(--card));
  }
  .bar.after b { background: var(--accent); }
  .bar > strong {
    text-align: right;
    font: 600 9px var(--mono);
    color: var(--ink);
  }
  .bar.unavailable b {
    width: 100%;
    background:
      repeating-linear-gradient(
        135deg,
        var(--rule) 0 3px,
        var(--paper-warm) 3px 6px
      );
  }
  .bar.unavailable > strong { color: var(--muted); }
  .verdict {
    justify-self: end;
    padding: 3px 6px;
    border: 1px solid rgba(36, 131, 123, 0.28);
    border-radius: 999px;
    color: var(--accent-deep);
    background: var(--accent-tint);
    font: 8.5px var(--mono);
    white-space: nowrap;
  }
  .verdict.changed {
    border-color: color-mix(in srgb, var(--accent) 55%, var(--rule));
    font-weight: 700;
  }
  figcaption {
    padding: 14px 24px 18px;
    border-top: 1px solid var(--rule);
    background: var(--paper);
    font-size: 12.5px;
    line-height: 1.55;
    color: var(--muted);
  }
  @media (max-width: 840px) {
    .results { margin-left: 0; max-width: none; }
    .stats { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .stat:nth-child(2) { border-right: 0; }
    .stat:nth-child(-n+2) { border-bottom: 1px solid var(--rule-soft); }
  }
  @media (max-width: 600px) {
    .curve-head { align-items: flex-start; flex-direction: column; gap: 5px; }
    .reader-row {
      grid-template-columns: 48px minmax(0, 1fr);
      gap: 10px;
    }
    .verdict {
      grid-column: 2;
      justify-self: start;
    }
  }
  @media (max-width: 480px) {
    .stats { grid-template-columns: 1fr; }
    .stat { border-right: 0; border-bottom: 1px solid var(--rule-soft); }
    .stat:last-child { border-bottom: 0; }
  }
</style>
