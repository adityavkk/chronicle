You are a research worker in an Orca multi-agent workflow, running in the git worktree /Users/auk000v/orca/workspaces/chronicle/event-sourcing-research (a branch of the "chronicle" durable-streams repo).

YOUR JOB: produce exactly ONE grounded, source-cited research markdown file. Do NOT modify any other file, do NOT build/run the chronicle code, do NOT touch git. Only create your assigned file.

GROUNDING (REQUIRED): Use the `/search` skill (the search SDK — invoke the `search` skill) to ground every non-obvious claim in PRIMARY sources: official documentation, maintainer design docs/papers, and authoritative posts by the maintainers. Cite sources inline as markdown links [text](url). Prefer official docs over secondary blogs. If sources conflict or you are unsure, say so explicitly rather than guessing. Do at least 4-6 distinct searches and fetch the key pages.

CONTEXT (READ FIRST): read research/00-chronicle-ground-truth.md in this worktree. It describes "Chronicle" — a durable append-only HTTP stream primitive on Redis (NOT an event-sourcing framework; no projections/snapshots/aggregates/bundled-handlers today). Your final section connects findings back to Chronicle.

REQUIRED STRUCTURE (H2 sections, in this order):
1. Model — the event/stream model; how events are identified, typed, ordered; what a "stream"/"aggregate" maps to.
2. Optimistic concurrency — the "expected version" mechanism; how write conflicts are detected and surfaced.
3. Snapshots — built-in or app-level; how state is snapshotted; how recovery folds snapshot + tail.
4. Projections / read models — what they are and WHEN computed (write-time/inline vs async/background vs read-time/on-the-fly); checkpoints/offsets; replay/rebuild.
5. Handlers & consumers — are command/event handlers bundled with the entity/stream? the subscription/consumer model (push vs pull, competing consumers, checkpointing, at-least-once vs exactly-once).
6. Lessons for Chronicle — 4-6 sharp, specific, opinionated bullets: what design pressure this system implies for whether/how Chronicle should grow toward event-sourced entities.

STYLE: tight and information-dense, ~250-500 lines. Quote short API signatures/snippets where illuminating. No filler, no marketing. Plain words.

WHEN DONE: report worker_done to the coordinator exactly as your dispatch preamble instructs, and put the output file path in the body.

YOUR ASSIGNMENT:
SYSTEMS: the LOG family + client-side projections + durable execution. THREE parts. OUTPUT FILE: research/04-log-tanstack-durable.md

(A) LOG-AS-SOURCE-OF-TRUTH (Kafka, Pulsar) — the family Chronicle most resembles:
- Kafka: topics/partitions/offsets; consumer groups + committed offsets (the checkpoint); LOG COMPACTION (key-based retention => a "latest value per key" table); Kafka Streams (KTable, state stores / RocksDB, interactive queries) and ksqlDB materialized views; the STREAM-TABLE DUALITY.
- Pulsar: topics; subscription modes (exclusive / shared / failover / key_shared); Pulsar Functions + Pulsar IO (compute/handlers ATTACHED to topics — bundled handlers, the analog of Akka's per-entity handlers but at topic granularity); topic compaction; tiered storage.
- The conceptual frame, with citations: Jay Kreps "The Log: What every software engineer should know about real-time data's unifying abstraction", and Martin Kleppmann "Turning the database inside out" (a database = a materialized view over a log; the event log is the source of truth; derived views are recomputable). Cite both.
- Why a log is NOT an event store: no per-entity streams, no per-entity expected-version, no aggregate boundary — you bolt CQRS/aggregates on top yourself. State this crisply; it is Chronicle's exact situation.

(B) TanStack DB as a CLIENT-SIDE PROJECTION engine:
- Collections (typed row sets); LIVE QUERIES backed by DIFFERENTIAL DATAFLOW (the d2ts engine — incremental view maintenance; recompute only deltas, not the whole aggregate); query/derived collections; optimistic mutations + transactions (instant local apply, then sync, rollback on failure); sync/electric/query collections backed by a server (e.g. ElectricSQL shapes).
- Frame it precisely as: the read model / projection moved to the CLIENT and kept incrementally up to date. This is the direct answer to the user's intuition that "TanStack DB is interesting for projections" and "you don't recompute the aggregate each time." Cite the TanStack DB docs.

(C) DURABLE EXECUTION adjacency (~half a section, brief): Restate (VIRTUAL OBJECTS = single-writer keyed handlers over a durable log, with journaled/replayed execution), Temporal (workflow event history + replay == event sourcing of a workflow), and DBOS in one line. Point: Chronicle's idempotent-producer + leased pull-wake worker model is adjacent to durable execution; a "virtual object" is essentially an event-sourced actor fronting a log. Cite Restate and Temporal docs.

Tie the three together in "Lessons for Chronicle": Chronicle is in the LOG family (A); TanStack DB (B) shows WHERE a projection can live (client, incremental) so Chronicle may not need server-side projections at all; durable execution (C) shows the "keyed single-writer handler over a log" pattern as another way to get event-sourced entities.
