# Rendered per-run artifacts and Terraform state — generated, not committed.
out/
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.auto.tfvars

# load-test run artifacts (regenerated per run)
gate2-*.txt
gate2-results.tsv
gate1-results.tsv
