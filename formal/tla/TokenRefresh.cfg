\* Positive run: the in-band self-heal makes [](Expired ~> Usable) hold under
\* weak fairness of Ack (issue #77). No CONSTRAINT — the action guards bound the
\* state space, and a CONSTRAINT can mask a real leads-to violation during
\* liveness checking (Specifying Systems sec 14.3.5).
\* Run: java -cp tla2tools.jar tlc2.TLC -config TokenRefresh.cfg TokenRefresh.tla
SPECIFICATION Spec
CONSTANTS
    TTL       = 3
    Threshold = 1
    MaxTime   = 8
INVARIANT TypeOK
PROPERTY  ExpiredLeadsToUsable
