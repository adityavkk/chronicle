\* Safety lane for the Membership convergence spec (issue #40): the per-state
\* and action safety invariants over the full churn+reconcile interleaving.
\* No fairness needed for safety; the full Next (incl. Join/Crash/Tick) runs.
SPECIFICATION SpecNoFair

CONSTANTS
    Replicas <- MCReplicas
    Slots <- MCSlots
    Score <- MCScore
    MemberTTL = 2
    SlotTTL = 2
    MaxEpoch = 8
    MaxChurn = 4

INVARIANT Inv
PROPERTY EpochMonotoneProp
PROPERTY TransferBumpsEpochProp

CONSTRAINT StateConstraint
