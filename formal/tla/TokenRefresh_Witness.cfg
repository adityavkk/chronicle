\* Non-vacuity witness: the Expired state MUST be reachable, so the leads-to in
\* TokenRefresh.cfg is checked on genuine expired states. TLC MUST report that
\* NeverExpired is violated (the Makefile asserts the violation).
SPECIFICATION Spec
CONSTANTS
    TTL       = 3
    Threshold = 1
    MaxTime   = 8
INVARIANT NeverExpired
