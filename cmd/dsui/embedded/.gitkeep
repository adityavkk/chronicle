# Vite build output lands here (see ../../ui/vite.config.ts). Generated; not committed.
# This placeholder keeps the directory tracked so `//go:embed all:embedded` compiles
# on a fresh checkout (vite's emptyOutDir removes it on each build; restore if missing).
