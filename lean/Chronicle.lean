import Chronicle.Producer
import Chronicle.Offset
import Chronicle.OffsetGreater

/-!
# Chronicle pure-core transcriptions (P0.5 skeleton)

Umbrella import for the typed transcription of Chronicle's pure cores.
Importing this module pulls in all three transcribed cores so
`lake build` compiles every module. No theorems are stated in this
package — proofs land in P1.1 (the pure-core-proofs issue).
-/
